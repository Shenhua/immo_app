services:
  app-immo:
    build: .
    container_name: app-immo
    ports:
      - "8502:8502"
    volumes:
      - ./results:/app/results
      - ./data:/app/data
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
    restart: unless-stopped
